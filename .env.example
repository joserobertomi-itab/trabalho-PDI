# Docker Compose environment (copy to .env and adjust).

# Default in this repo: bundled base under data/
DATA=./data/Train_and_Validation

# Assignment/submission layout (real folder — do not use a symlink with Docker):
# DATA=./dataset

OUT=./resultado
CALIB=./calibration
LIMIT=3
PORT=8765
