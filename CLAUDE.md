# Claude / AI entry

Read **`AGENTS.md`** first. This repo uses the `.agents/` harness + `.cursor/skills/` — not spec-driven development.

Domain: `CONTEXT.md`. Constraints: `.agents/constraints.md`.
